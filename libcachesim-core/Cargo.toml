[package]
name = "libcachesim-core"
version = "0.1.0"
edition = "2021"
authors = ["libCacheSim Rust Port Team"]
license = "GPL-3.0"

[dependencies]
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }

